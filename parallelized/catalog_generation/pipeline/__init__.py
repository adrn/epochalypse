"""Stage implementations for the epochalypse catalog-generation pipeline.

Nothing scientific is decided here: every prior, threshold, path, and seed is
passed in from the CONFIGURATION block at the top of `generate_catalog.py`.
"""
